
{{#each posts as |post|}}
<div class="card">
    <div class="card-header"><a href="/view/{{post.id}}">{{post.title}}</a></div>
    <div class="card-body p-5">
        {{post.content}}
    </div>
</div>
{{/each}}