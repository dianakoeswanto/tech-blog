<div class="card">
    <div class="card-header">Edit Post</div>
    <div class="card-body p-5">
        <form class="form form-edit">
            <input type="hidden" id="postId" value="{{post.id}}"/>
            {{> post-details}}
            <button type="button" style="width: 100%" class="btn btn-danger mb-3" id="btnDelete">Delete</button>
            <a class="btn btn-secondary" style="width: 100%" id="btnCancel mb-3" href="/dashboard">Cancel</a></button>
        </form>
    </div>
</div>

<script src="/js/edit.js"></script>